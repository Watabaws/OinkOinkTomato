#include <stdio.h>

int cat_strn(char * dest, char * source, int n){
    int i;
    int og_len = str_len(dest);
    for(i = 0; i < n; i++){
        dest[og_len + i] = source[i];
    }
    dest[og_len + i + 1] = 0;
    return dest;

}
